Я добавил переменную termn пустую в state в файле app.js рядом с данными

а потом деструктуризировал эти два стейта const {data, term} = this.state

далее создал функцию которая принимает items и term , на term поставил условие на пустое значение (term === 0) и если это так то отдает items

А потом return и фильтр на items.filter(item => { return item.name.indexOf(term) > -1})

А ещё после деструктуризации я передавал уже вместе this.state.data просто data 

---------------------

