Переделываю компонент на класс, после импортирую компонент с реакта для 
класса (именно его поведение)

Мне надо было вырезать JSX и добавить всунуть его в return 

Добавляю переменные под стредством вытаскивания их из props 

const {name, salary, increase} = this.props;

Это я должен был сделать внутри render , 

по позже создать конструктор с super 

construstor(props) {
    super(props);
    this.state = {
        increase: false;
    }
}

На первую кнопку внутри div буду назначать обработчик событий onClick и туда буду передавать метод onIncrease

onIncrease = () => {
    this.setState(({increase}) => {
        increase: !increase;
    })
}

От будет при клике возвращаться противоположное значение 

и потом создаю переменную внутри render, которая будет деструктуризирована и иметь в себе 
внутрее состояние 

Пока что эти данные не отслеживаются, просто верстка ничего не меняется 

